// ─── index.js ────────────────────────────────────────────────────────────────
// Entry point — just starts the server.
// server.js handles pairing + auto-launching bots per user.
// src/botRunner.js handles running each user's bot instance.

import './server.js';
